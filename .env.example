# =====================================================
# E-Commerce API - Environment Variables
# =====================================================
# Copy this file to .env and update with your values
# NEVER commit .env to version control!
# =====================================================

# -----------------------------------------------------
# Database Configuration
# -----------------------------------------------------
# Format: mysql+pymysql://username:password@host:port/database
DATABASE_URL=mysql+pymysql://root:YOUR_MYSQL_PASSWORD@localhost/ecommerce_db

# For remote database:
# DATABASE_URL=mysql+pymysql://user:pass@remote-host:3306/ecommerce_db

# -----------------------------------------------------
# Security Configuration
# -----------------------------------------------------
# IMPORTANT: Generate secure random keys for production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(32))"

# Main secret key for JWT tokens
SECRET_KEY=change-this-to-a-secure-random-string-in-production

# Secret key for password reset tokens
RESET_SECRET_KEY=change-this-to-another-secure-random-string

# -----------------------------------------------------
# JWT Configuration
# -----------------------------------------------------
# Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=60

# JWT Algorithm (don't change unless you know what you're doing)
ALGORITHM=HS256

# -----------------------------------------------------
# Application Configuration
# -----------------------------------------------------
# Environment: development, staging, production
ENVIRONMENT=development

# API Host and Port
API_HOST=0.0.0.0
API_PORT=8000

# Enable/disable debug mode
DEBUG=true

# -----------------------------------------------------
# CORS Configuration
# -----------------------------------------------------
# Allowed origins (comma-separated for multiple origins)
# Use * for all origins in development only!
CORS_ORIGINS=*

# For production, specify exact origins:
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# -----------------------------------------------------
# Optional: Email Configuration (for password reset emails)
# -----------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourapi.com

# -----------------------------------------------------
# Optional: Redis Configuration (for caching)
# -----------------------------------------------------
# REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------
# Optional: File Upload Configuration
# -----------------------------------------------------
# MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
# UPLOAD_FOLDER=uploads/

# -----------------------------------------------------
# Optional: Logging Configuration
# -----------------------------------------------------
# LOG_LEVEL=INFO
# LOG_FILE=logs/api.log

# =====================================================
# Security Notes:
# =====================================================
# 1. Never use default values in production
# 2. Generate strong random secret keys
# 3. Use strong database passwords
# 4. Keep this file secure and never commit it
# 5. Rotate keys regularly in production
# 6. Use environment-specific configurations
# =====================================================